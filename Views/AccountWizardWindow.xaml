<Window x:Class="CodexEnvironmentManager.Views.AccountWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Add Account" Height="420" Width="520"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#0d1117" Foreground="#c9d1d9">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="140"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Text="Add Account" FontSize="18" FontWeight="Bold" Grid.Row="0" Grid.ColumnSpan="2" Margin="0,0,0,12"/>

        <TextBlock Text="Provider" Grid.Row="1" Grid.Column="0" VerticalAlignment="Center" Margin="0,6"/>
        <ComboBox x:Name="ProviderBox" Grid.Row="1" Grid.Column="1" Margin="0,4"/>

        <TextBlock Text="Authentication" Grid.Row="2" Grid.Column="0" VerticalAlignment="Center" Margin="0,6"/>
        <ComboBox x:Name="AuthTypeBox" Grid.Row="2" Grid.Column="1" Margin="0,4"/>

        <TextBlock Text="Account name" Grid.Row="3" Grid.Column="0" VerticalAlignment="Center" Margin="0,6"/>
        <TextBox x:Name="NameBox" Grid.Row="3" Grid.Column="1" Margin="0,4"/>

        <TextBlock x:Name="ApiKeyLabel" Text="API Key" Grid.Row="4" Grid.Column="0" VerticalAlignment="Center" Margin="0,6" Visibility="Collapsed"/>
        <PasswordBox x:Name="ApiKeyBox" Grid.Row="4" Grid.Column="1" Margin="0,4" Visibility="Collapsed"/>

        <TextBlock Grid.Row="5" Grid.ColumnSpan="2" Margin="0,8" Foreground="#8b949e" TextWrapping="Wrap"
                   Text="Select the provider and authentication type for this account. Codex accounts keep existing Codex behavior. Kimi accounts are isolated per account using KIMI_CODE_HOME."/>

        <StackPanel Grid.Row="6" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12">
            <Button Content="Save" Width="90" Margin="0,0,8,0" Click="Save_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="90" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>

        <TextBlock x:Name="StatusText" Grid.Row="7" Grid.ColumnSpan="2" Text="" Foreground="#f85149" TextWrapping="Wrap"/>
    </Grid>
</Window>
