<Window x:Class="CodexEnvironmentManager.Views.InputDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Input" Height="170" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        Background="#0d1117" Foreground="#c9d1d9">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="PromptText" Text="Enter value:" Grid.Row="0" Margin="0,0,0,5" TextWrapping="Wrap"/>
        <Grid Grid.Row="1" Margin="0,0,0,10">
            <TextBox x:Name="InputText"/>
            <PasswordBox x:Name="SecretInput" Visibility="Collapsed"/>
        </Grid>
        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="OK" Width="70" Margin="0,0,5,0" Click="Ok_Click" IsDefault="True"/>
            <Button Content="Cancel" Width="70" Click="Cancel_Click" IsCancel="True"/>
        </StackPanel>
    </Grid>
</Window>
